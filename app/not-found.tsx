import { NothingHere } from "@/components/not-found-views";

/** Any URL that matches no route, and any `notFound()` with no closer file. */
export default function NotFound() {
  return <NothingHere />;
}
