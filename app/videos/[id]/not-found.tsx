import { MissingVideo } from "@/components/not-found-views";

/** `/videos/<id>`: never issued, malformed, or another account's. */
export default function VideoNotFound() {
  return <MissingVideo />;
}
