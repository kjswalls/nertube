# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — real secrets live in .env.local, which is never committed.
# .env.example holds placeholder names only and IS committed.
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# supabase CLI local state
/supabase/.temp
/supabase/.branches

# local dev stack (scripts/dev-stack): throwaway database bytes, PostgREST
# config and uploaded objects. Never committed.
/.dev-stack/

# Vitest's on-disk cache (vitest 5 writes it beside the config, not in
# node_modules). Build output, never source.
/.vitest/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/e2e/screenshots/
